import { Fab, styled } from "@mui/material";

export const AddExerciseFabStyled = styled(Fab)({
    position: 'fixed',
    zIndex: 1,
    bottom: 30,
    left: 0,
    right: 0,
    margin: '0 2rem 0 auto',
})
